
${if starting-kernels-agents}
echo "Starting Kernels/Agents"
${starting-kernels-agents}

echo "Checking if Kernels/Agents run"
sleep 10

${check-kernels-agents}
${end}

echo "Starting Master"
${starting-master}
